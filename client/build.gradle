buildscript {
    repositories {
        mavenCentral()
        maven {
            name = "forge"
            url = "https://files.minecraftforge.net/maven"
        }
        maven {
            url "https://jitpack.io"
        }

    }
    dependencies {
        classpath "com.github.asbyth:ForgeGradle:b13624b272"
        classpath "com.github.Skytils:mixingradle:d5f9873d60"
    }
}

apply plugin: "net.minecraftforge.gradle.tweaker-client"
apply plugin: "org.spongepowered.mixin"

minecraft {
    version = "1.8.9"
    tweakClass = "org.bookmc.tweaker.client.BookMCClientLoader"
    mappings = "stable_22"
    runDir = "run"

    makeObfSourceJar = false
}

repositories {
    maven {
        url "https://repo.spongepowered.org/repository/maven-public/"
    }
}

dependencies {
    embed("org.spongepowered:mixin:0.8.3-SNAPSHOT") {
        exclude module: "commons-io"
        exclude module: "launchwrapper"
        exclude module: "log4j-core"
    }
    embed(project(":common"))

    compileOnly "org.ow2.asm:asm-tree:9.1"
    compileOnly "org.ow2.asm:asm-commons:9.1"
    compileOnly "org.ow2.asm:asm-util:9.1"

    compileOnly "com.google.guava:guava:21.0"

    annotationProcessor("org.spongepowered:mixin:0.8.3-SNAPSHOT:processor") {
        exclude module: "commons-io"
        exclude module: "launchwrapper"
        exclude module: "log4j-core"
    }
}

mixin {
    // Specify "notch" or "searge" here
    defaultObfuscationEnv notch
    add sourceSets.main, "mixin.bookmc-client.refmap.json"
}

jar {
    manifest.attributes("MixinConfigs": "bookmc-client.mixins.json")

    configurations.embed.each { dep ->
        from(project.zipTree(dep)) {
            exclude "META-INF", "META-INF/**"
            exclude "org/jetbrains/annotations", "org/jetbrains/annotations/**"
        }
    }
}

tasks.jar.finalizedBy(tasks.reobfJar)