buildscript {
    repositories {
        mavenCentral()
        maven {
            name = "forge"
            url = "https://files.minecraftforge.net/maven"
        }
        maven {
            url "https://jitpack.io"
        }

    }
    dependencies {
        classpath "com.github.asbyth:ForgeGradle:b13624b272"
        classpath "com.github.Skytils:mixingradle:d5f9873d60"
    }
}

apply plugin: "net.minecraftforge.gradle.tweaker-server"
apply plugin: "org.spongepowered.mixin"
apply plugin: "java-library"

minecraft {
    version = "1.8.9"
    tweakClass = "org.bookmc.loader.impl.tweaker.BookMCClientLoader"
    mappings = "stable_22"
    runDir = "run"
    makeObfSourceJar = false
}

repositories {
    maven {
        url "https://repo.spongepowered.org/repository/maven-public/"
    }
}

dependencies {
    include("org.spongepowered:mixin:0.7.11-SNAPSHOT") {
        exclude module: "guava"
        exclude module: "commons-io"
        exclude module: "gson"
        exclude module: "launchwrapper"
        exclude module: "log4j-core"
    }

    include(project(":common"))

    annotationProcessor("org.spongepowered:mixin:0.7.11-SNAPSHOT") {
        exclude module: "commons-io"
        exclude module: "launchwrapper"
        exclude module: "log4j-core"
    }
}

jar {
    manifest.attributes(
        "TweakClass": "org.bookmc.tweaker.server.BookMCServerLoader",
        "TweakOrder": 0,
        "Manifest-Version": 1.0
    )
}

tasks.build.dependsOn(shadowJar)