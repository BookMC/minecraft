allprojects {
    apply plugin: "java"

    sourceCompatibility = targetCompatibility = 1.8
    compileJava.options.encoding = "UTF-8"

    version = "0.0.1"
    group = "org.bookmc"

    repositories {
        maven {
            url "https://jitpack.io"
        }
        mavenCentral()
    }

    configurations {
        embed
        implementation.extendsFrom(embed)
    }

    dependencies {
        compileOnly "com.github.BookMC:services:293f5877c7"
        compileOnly "com.github.BookMC:tweaker:d447428f93"
        compileOnly "com.github.BookMC:loader:1ad7c87ddb"
    }

    processResources {
        inputs.files "src/main/resources"
        outputs.dir "build/classes/main"
        copy {
            from("src/main/resources")
            into("build/classes/main")
        }
    }

    jar {
        from {
            configurations.embed.collect {
                it.isDirectory() ? it : zipTree(it)
            }
        }

        duplicatesStrategy DuplicatesStrategy.EXCLUDE

        exclude "META-INF/*.RSA", "META-INF/*.SF", "META-INF/*.DSA", "module-info.class"
    }
}